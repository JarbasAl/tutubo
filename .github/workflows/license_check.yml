name: License Check
on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  license_check:
    uses: OpenVoiceOS/gh-automations/.github/workflows/license_check.yml@dev
    with:
      package_name: tutubo
