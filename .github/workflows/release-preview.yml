name: Release Preview
on:
  pull_request:
    types: [opened, synchronize, labeled, unlabeled]

jobs:
  release_preview:
    uses: OpenVoiceOS/gh-automations/.github/workflows/release-preview.yml@dev
    with:
      version_file: tutubo/version.py
